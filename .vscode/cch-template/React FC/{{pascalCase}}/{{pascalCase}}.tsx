import { memo } from 'react';

interface {{pascalCase}}Props {}

export const {{pascalCase}} = memo((props: {{pascalCase}}Props) => {
    const {} = props;
    return (
        <div>
            {{pascalCase}}
        </div>
    );
});